# tests/docker-compose.tests.local.yml
# This override file has only one purpose: to make the nether-bridge
# container run as the root user for local Docker Desktop testing.
services:
  nether-bridge:
    user: "root"