# tests/docker-compose.tests.yml
services:
  nether-bridge:
    image: nether-bridge-test:local
    container_name: nether-bridge
    build:
      context: ../
      dockerfile: Dockerfile
    restart: "no"
    networks:
      - mc-network
    ports:
      - "19132:19132/udp"
      - "25565:25565/udp"
      - "25565:25565/tcp"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - LOG_LEVEL=DEBUG
      - NB_IDLE_TIMEOUT=30
      - NB_PLAYER_CHECK_INTERVAL=5
    healthcheck:
      test: ["CMD", "python", "nether_bridge.py", "--healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  mc-bedrock:
    image: itzg/minecraft-bedrock-server:latest
    restart: "no"
    container_name: mc-bedrock
    networks:
      - mc-network
    volumes:
      - mc-bedrock-data:/data
    environment:
      - EULA=TRUE

  mc-java:
    image: itzg/minecraft-server:latest
    restart: "no"
    container_name: mc-java
    networks:
      - mc-network
    volumes:
      - mc-java-data:/data
    environment:
      - EULA=TRUE

  # This service now specifically builds the 'testing' stage
  tester:
    build:
      context: ../
      dockerfile: Dockerfile
      # This line tells Docker to build the 'testing' stage from the Dockerfile
      target: testing
    networks:
      - mc-network
    volumes:
      - ../:/app
      - /var/run/docker.sock:/var/run/docker.sock
    # This overrides the Dockerfile's ENTRYPOINT so we can run pytest
    entrypoint: ""
    depends_on:
      nether-bridge:
        condition: service_healthy

networks:
  mc-network:
    driver: bridge

volumes:
  mc-bedrock-data:
  mc-java-data: